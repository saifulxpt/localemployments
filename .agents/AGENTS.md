# Workspace Rules for localemployments

## Git Directives
- **Auto-push changes**: Every time a task or code update is completed, stage the modified/new files, create a descriptive commit message, and push to GitHub repository (`origin main`).
